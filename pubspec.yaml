name: eduquiz
description: "A Flutter EduQuiz app with authentication, room codes, lobby, realtime quiz, leaderboard, review, and host dashboard."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

flutter_animate:

google_fonts:

fl_chart:

lottie: